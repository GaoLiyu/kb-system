// icons.ts
import {
  DataLine,
  FolderOpened,
  Document,
  EditPen,
} from '@element-plus/icons-vue';

export const iconMap: Record<string, any> = {
  DataLine,
  FolderOpened,
  Document,
  EditPen,
};